# Screenshots

Add deployment screenshots here after the service is running:

- `dashboard.png`: Railway or Render deployment dashboard.
- `running.png`: service health/readiness endpoint working.
- `test.png`: authenticated `/ask` request working.

